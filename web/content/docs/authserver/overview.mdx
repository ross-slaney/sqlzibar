---
title: "AuthServer Overview"
description: "Identity, sessions, organizations, and SSO in one embedded module."
order: 0
section: authserver
---

<Banner
  eyebrow="Identity inside your app"
  title="Users, sessions, SSO, OAuth, and social OIDC from one embedded module"
  href="../getting-started"
  actionLabel="Install SqlOS"
>
  AuthServer is the identity half of SqlOS. It keeps your users, clients, sessions, and login flows in your app and in your database instead of splitting them into another hosted dependency.
</Banner>

AuthServer is SqlOS's identity piece. Users, orgs, sessions, OAuth tokens, social OIDC, and SAML run inside your app. Data lives in your database. For applications, SqlOS is both an OAuth authorization server and an [OpenID Connect Provider](/docs/authserver/openid-provider): it mints access and refresh tokens, and it issues ID tokens, OIDC discovery, and UserInfo to downstream relying parties. Toward Google, Microsoft, Apple, and custom social login it plays the opposite role — SqlOS is the OIDC relying party of those upstream providers.

<Callout type="info" title="Recommended reading order">
  1. The standard flow: [Single application](/docs/authserver/single-application) — one `AddSqlOS` call with `Origin`, `Api`, `Mcp`, `Brand` or `Headless`, and `Authorization`
  2. [Dashboard](/docs/authserver/dashboard-overview) → [Organizations](/docs/authserver/organizations) → [Hosted vs headless](/docs/authserver/hosted-vs-headless)
  3. [Password login](/docs/authserver/password-login), [Email OTP](/docs/authserver/email-otp), [Magic Link](/docs/authserver/magic-link), or [Phone OTP](/docs/authserver/phone-otp) → [OIDC](/docs/authserver/oidc-auth) / [SAML](/docs/authserver/saml-sso) / [SCIM](/docs/authserver/scim-directory-sync)
  4. Security policy: [MFA and TOTP](/docs/authserver/mfa-totp) and [Security Settings](/docs/authserver/security-settings)
  5. Many apps: [Multiple applications](/docs/authserver/multiple-applications), then [Clients](/docs/authserver/clients), [OpenID Provider](/docs/authserver/openid-provider), [Machine Clients](/docs/authserver/machine-clients), [Application Access](/docs/authserver/application-access), [CIMD](/docs/authserver/client-id-metadata-documents), [DCR](/docs/authserver/dynamic-client-registration) -- only when you need them
</Callout>

## What you get

<CardGrid cols={3}>
  <Card title="Organizations" description="Multi-tenant orgs, memberships, and roles." />
  <Card title="Users" description="Password, email code, magic link, phone code, SSO-provisioned, or OIDC-linked identities." />
  <Card title="Sessions" description="JWT access tokens, refresh token rotation, and replay detection." />
  <Card title="MFA and recovery codes" description="Optional self-enrollment, tenant-required TOTP, and recovery-code challenges." />
  <Card title="OIDC" description="Google, Microsoft, GitHub, Apple, and custom providers." />
  <Card title="SAML SSO" description="Enterprise SSO with provisioning and hosted flows." />
  <Card title="SCIM" description="Directory provisioning, deprovisioning, and FGA group sync." />
  <Card title="Invitations" description="Email-bound organization invites with hosted, headless, and SDK acceptance." />
  <Card title="CLI OAuth" description="Device Authorization Grant for terminal apps and remote sessions." />
  <Card title="Hosted or headless" description="Use the built-in pages or render your own UI on top of the same runtime." />
</CardGrid>

## Setup

```csharp
builder.AddSqlOS<AppDbContext>(
    db => db.UseSqlServer(connectionString),
    options => options.UseSingleApplication("Acme", app => app.Origin = "https://acme.example.com"));

var app = builder.Build();
```

That is the whole registration for one application; the auth server, hosted pages, and dashboard are mapped at startup. Add `Api`, `Mcp`, `Brand` or `Headless`, and `Authorization` inside the same call as the product needs them ([Single application](/docs/authserver/single-application)). Hosts that serve several applications seed clients explicitly instead ([Multiple applications](/docs/authserver/multiple-applications)).

## Auth flow

Typical browser login:

1. App sends the user to `/sqlos/auth/authorize` with PKCE.
2. User chooses an enabled credential. Email-first flows use [home realm discovery](/docs/authserver/home-realm-discovery) to pick password, Email OTP, a magic link, or SSO.
3. User signs in (password, email or phone OTP, magic link, OIDC, or SAML).
4. Multiple orgs? User picks one.
5. MFA required? SqlOS renders or returns a TOTP challenge or forced enrollment step.
6. SqlOS returns an auth code.
7. App trades the code for access and refresh tokens.

<Callout type="tip" title="Start with owned clients first">
  For most teams, the first production flow should use a seeded or dashboard-created owned client. Add `CIMD`, `DCR`, and resource indicators only when you actually need portable or compatibility clients.
</Callout>

![Hosted sign-in page](/docs/auth-signin-page.png)

## Dashboard

Open `/sqlos/admin/auth/`. Manage orgs, users, memberships, clients, OIDC, SSO, security, and sessions there.

![Dashboard users](/docs/dashboard-users.png)

## SDK services

| Service | Purpose |
| --- | --- |
| `SqlOSAuthService` | Login, email and phone OTP, magic links, invitations, refresh, logout, token validation |
| `SqlOSAdminService` | Create orgs, users, memberships, clients, SSO |
| `SqlOSCryptoService` | Token generation, PKCE, password hashing, JWKS |
| `SqlOSHomeRealmDiscoveryService` | Route users to password or SSO by email domain |
| `SqlOSSsoAuthorizationService` | SAML SSO authorization and code exchange |
| `SqlOSOidcAuthService` | Google, Microsoft, GitHub, Apple, and custom social login |
| `SqlOSSettingsService` | Session lifetimes and security configuration |

## Hosted and headless

<Accordion>
  <AccordionItem title="Hosted auth" defaultOpen>
    SqlOS renders the login, signup, invitation, OTP, and recovery pages for you. This is the fastest path for internal tools and most first-party apps.
  </AccordionItem>
  <AccordionItem title="Headless auth">
    SqlOS still runs OAuth authorization, sessions, token issuance, and its social OIDC relying-party flows. Your application takes ownership of the login UI and interaction flow.
  </AccordionItem>
  <AccordionItem title="Client onboarding modes">
    Use seeded or dashboard-created owned clients for the normal path. Reach for CLI device flow, `CIMD`, and optional `DCR` when you need terminal, portable, or compatibility-oriented clients.
  </AccordionItem>
</Accordion>

[Headless Auth](/docs/authserver/headless-auth) · [Email OTP](/docs/authserver/email-otp) · [Magic Link](/docs/authserver/magic-link) · [Phone OTP](/docs/authserver/phone-otp) · [MFA and TOTP](/docs/authserver/mfa-totp) · [Email Invitations](/docs/authserver/invitations) · [SAML SSO](/docs/authserver/saml-sso) · [SCIM Directory Sync](/docs/authserver/scim-directory-sync) · [CLI OAuth](/docs/authserver/cli-oauth) · [Hosted vs Headless](/docs/authserver/hosted-vs-headless)

## Client guides

- [Single-Application Setup](/docs/authserver/single-application)
- [Clients](/docs/authserver/clients)
- [Machine Clients](/docs/authserver/machine-clients)
- [Application Access](/docs/authserver/application-access)
- [Preregistration vs CIMD vs DCR](/docs/authserver/preregistration-vs-cimd-vs-dcr)
- [Client ID Metadata Documents](/docs/authserver/client-id-metadata-documents)
- [Dynamic Client Registration](/docs/authserver/dynamic-client-registration)
- [CLI OAuth](/docs/authserver/cli-oauth)
- [MCP Server](/docs/authserver/mcp-server)
- [MCP Resource Indicators and Audience](/docs/authserver/mcp-resource-indicators-and-audience)
